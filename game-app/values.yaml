# Update the auth path in ingress to use the correct port (4001 instead of 80)
server:
  image:
    repository: "ghcr.io/isirajieinnocent/spacetime-db"
    tag: "latest"
ingress:
  enabled: true
  className: "nginx"
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/rewrite-target: "/"
  hosts:
    - host: "play.text2banger.fun"
      paths:
        - path: "/"
          pathType: "Prefix"
          backend:
            serviceName: "client"
            servicePort: 80
        - path: "/auth"
          pathType: "Prefix"
          backend:
            serviceName: "auth"
            servicePort: 4001  # ← CHANGE THIS FROM 80 TO 4001
        - path: "/server"
          pathType: "Prefix"
          backend:
            serviceName: "server"
            servicePort: 80
  tls:
    - secretName: "play-text2banger-tls"
      hosts:
        - "play.text2banger.fun"
